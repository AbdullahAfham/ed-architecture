<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="hr_payslip_inherit_form_view" model="ir.ui.view">
        <field name="name">hr.payslip.inherit.form</field>
        <field name="model">hr.payslip</field>
        <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
        <field name="arch" type="xml">
            <field name="payslip_run_id" position="after">
                <field name="khr_currency_id" readonly="state != 'draft'"/>
                <field name="exchange_date" readonly="state != 'draft'"/>
                <field name="exchange_rate" readonly="state != 'draft'"/>
                <field name="payroll_type"/>
                <field name="first_paid" invisible="1"/>
                <field name="first_paid" invisible="payroll_type == 'first'"/>
            </field>
        </field>
    </record>
</odoo>
